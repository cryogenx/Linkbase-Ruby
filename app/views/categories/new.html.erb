<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
<h1>Add a new category</h1>
  <%= simple_form_for @category, url: {action: "create"} do |f| %>
  <%= f.input :name %>
  <%= f.input :comment %>
  <br />
  <%= f.submit "Create", :class => 'btn btn-primary' %>
  <br />
<% end %>
<br />
</div>